# External LLM service configuration
externalHost: "192.168.1.115"
port: 8080

# Gateway API configuration
gateway:
  name: default-gateway-ingress
  namespace: kgateway-system

hostname: llm.wheat-dn42.net
