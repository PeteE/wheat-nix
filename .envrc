#!/bin/sh

strict_env

use flake

if command -v lefthook >/dev/null 2>&1; then
  # Don't let a hook-install hiccup abort the whole shell under strict_env.
  lefthook install >/dev/null || echo "direnv: lefthook install failed (continuing)" >&2
fi
